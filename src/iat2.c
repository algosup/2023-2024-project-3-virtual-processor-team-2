#include <stdio.h>
#include <stdlib.h>

#include "builder.h"
#include "parser.h"
#include "runner.h"

int main(int argc, char *argv[]) {
    // TODO: make the function parseArgs

    // TODO: Init variables list struct

    // TODO: Init labels list struct

    // TODO: Init instructions list struct

    // run parser
    // TODO: make the function parseFile

    // run builder
    // TODO: make the function build

    // run runner
    // TODO: make the function run

    exit(EXIT_SUCCESS);
}